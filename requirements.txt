# Runtime dependencies are declared in pyproject.toml.
# This file exists for tooling that still expects it.
#
#   pip install -e ".[dev]"       development
#   pip install -e ".[qdrant]"    with the Qdrant memory backend

httpx>=0.27
PyYAML>=6.0
